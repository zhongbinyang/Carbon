# 架构图相关文档内容摘录

来源文档：

- `doc/3562276219使用说明书 TCB-NE(BD2.20AAF) User's Manual v1.20(1).pdf`
- `doc/AppNotes+TCB+系列温控板+MODBUS+通信协议技术文档+V1.00.pdf`
- `doc/AppNotes+TCB系列温控板通信协议+V2.05.pdf`
- `doc/阶梯科技测试系统--底层通信协议JTT1031.docx`

抽取文本位置：

- `.drawio-tmp/doc_extract/`

## 1. TCB-NE 温控板与通道架构

TCB-NE 是 TEC 半导体制冷片温度控制板，内含 PID 控制算法，连续输出，控温精度可达 0.01 度。支持 10K NTC，且可选 PT100/PT1000 温度传感器。

与架构图相关的硬件关系：

- `TCB-NE -> TEC`：TEC 接口为 H/L 两端，支持双向加热/制冷。
- `TCB-NE -> 温度传感器`：NTC/PT100/PT1000 反馈形成闭环 PID。
- `TCB-NE -> CTL`：提供 Ready、报警输出、TEC 使能/关闭输入。
- `TCB-NE -> TVDD`：电源输入 4.5V-27V，推荐根据 TEC 额定电压选择供电。
- `TCB-NE -> 通信总线`：通信接口按型号为 RS232 或 RS485。

图中建议补充：

- 把 TCB-NE 的 `CTL` 控制端子拆出为状态/联锁信号：`Ready`、`Alarm`、`TEC Enable/Disable`。
- 把温度反馈明确标注为 `NTC/PT100/PT1000`，不是普通温度采样。
- 保留 `TCB-NE *4`，但每个通道应表达为 `TCB-NE + TEC + NTC + Fan/散热器 + JTT1031 测试板`。

## 2. 电源与 TEC 选型约束

TCB-NE 手册中给出的关键约束：

- 板子供电电压：4.5V-27V，推荐 5V-24V 标准开关电源。
- TEC 电压：1V-24V，依供电电源电压而定。
- TEC 电流：额定 10A；当 TEC 电压小于等于 12V 时，电流可到 12A。
- 如果 TVDD 电压高于 TEC 额定电压，需要通过最大占空比 `U` 限制输出：`VTEC = VTVDD * U%`。
- 前级开关电源功率建议留裕量，手册提到 TEC 上电前几秒电流可能比标称额定电流大 30%以上。

图中建议补充：

- `LRS-600-24 *2` 不应只画成一个普通 24V 总线。应明确是并联供电、分区供电，还是一路给 TEC/风扇、一路给控制/测试板。
- 每个 TEC 通道建议标注分路保护或电流检测。
- 增加 `最大占空比 U / TEC 额定电压匹配` 作为设计约束注记。

## 3. 散热与温控机械链路

手册强调 TEC 热面散热能力决定最低温度与稳定性；散热不足会导致热面温度上升，最终让系统失控。NTC 应安装在对象温度最稳定的位置，不应太靠近 TEC、热源或环境空气。

图中建议补充：

- `TEC -> 散热器/风扇` 不只是辅助部件，而是温控闭环稳定性的关键路径。
- 在通道模板中增加 `被控金属夹具/热沉`，表达光模块夹具不是直接接 TEC 的唯一对象。
- 增加 `NTC 安装位置` 或 `夹具温度反馈` 标注。

## 4. TCB 通信协议与总线配置

TCB 支持 ASCII 与 MODBUS RTU。MODBUS 默认关闭，使用前需：

- 先通过 ASCII 指令 `T0` 关闭自发送，避免主动上报数据干扰 MODBUS。
- 发送 `SMS1` 使能 MODBUS。
- `SMS1` 后 CTL 接口的 `W` 脚由报警 IO 输出变为 MODBUS 配置输入。
- 工业现场建议短接 `W` 与 `G`，让温控板仅运行 MODBUS，避免总线干扰误触发 ASCII 模式。

MODBUS 物理/协议参数：

- 默认波特率 9600 bps，可选 9600 / 19200 / 38400 / 57600 / 115200。
- 数据位 8，停止位 1，无校验。
- 主机帧之间需要至少 5ms 停顿，建议请求-等待应答-再发下一请求。
- MODBUS 从站地址范围 128-247，默认地址 128；广播地址 0。

图中建议补充：

- 当前图里 `RS485/CAN 总线` 建议改为更确定的 `RS485 / MODBUS RTU`，除非另有 CAN 证据。
- 对 4 块 TCB-NE，应标注 `独立 Modbus 地址 128-247`、`9600bps 默认`、`5ms+轮询间隔`。
- 增加 `W-G 短接: 纯 MODBUS 模式` 或作为工程注记。
- 增加 `RS485 120Ω 终端电阻 JP0` 注记。

## 5. TCB 状态、报警与寄存器

与上位机/主控相关的核心状态：

- Ready：达到设定温度 +/-0.3 度后开始约 120 秒倒计时，结束后进入 Ready。
- Error：超温、长时间不就绪、探头短路、探头断开等会触发报警。
- CTL 的 Ready 输出为 3.3V 就绪信号；报警输出低电平为报警，高电平为正常。
- TEC 输出可通过 IO 或协议控制，但使用过协议 `SEN` 控制后，IO 控制会失效以避免冲突。

MODBUS 相关寄存器：

- DI 10001：就绪状态。
- DI 10002：报警状态。
- DO 00001：TEC 输出使能。
- DO 00002：系统复位。
- AI 30001：当前温度 PV，单位 0.01。
- AI 30002：目标温度 SV，单位 0.01。
- AI 30003：TEC 控制量 DUTY，负为制冷，正为加热，范围 +/-999。
- AI 30004：报警信息字。
- AO 40001：目标温度 SV。
- AO 40002：最大占空比 U。
- AO 40012：报警温差。
- AO 40015：本机 MODBUS 地址。
- AO 40016：本机波特率。

图中建议补充：

- `TCB-NE -> 上位机/主控` 的数据流建议标注：`PV/SV/DUTY/Ready/Error`。
- 把 `状态显示板 JTT1049V3` 与 `Ready/Alarm/通道使能` 关联起来。

## 6. JTT1031 / 光模块测试接口相关内容

DOCX 的底层通信协议包含通用光模块命令和 `0x1031/0x1030 - TEC-100G BERT 子卡`。

通用光模块相关命令包括：

- `0x0010` 查询光模块状态。
- `0x0011` 控制光模块供电。
- `0x0012` 读取光模块数据。
- `0x0013` 写入光模块数据。
- `0x0014` 读光模块内部温度。
- `0x0019` 控制光模块 `txdisable`。

`0x1031/0x1030 - TEC-100G BERT 子卡` 相关状态包括：

- `pin-power`：供电状态，1 有电，0 无电。
- `pin-in`：在位状态，1 在位，0 无。
- `opm-current`：光模块电流。
- `opm-voltage`：光模块电压。
- `pin-los`：LOS 引脚状态。
- PRBS 状态、误码数、误码率、PRBS 控制、速率和模式设置。

图中建议补充：

- `JTT1031V3 测试板` 不只是插槽，应拆成 `DUT 供电控制`、`I2C/EEPROM 读写`、`光模块状态采集`、`PRBS/BERT 测试接口`。
- 外部仪表若实际由 BERT/PRBS 子卡完成，图中“光功率计/误码仪”应改为“BERT/PRBS 测试链路或外部仪表”，避免误导。
- 增加从 `JTT1031V3` 到上位机/主控的数据：`pin-in / pin-power / current / voltage / LOS / module temperature / PRBS result`。

## 7. 对现有架构图的直接修订建议

优先级高：

1. 将 `RS485/CAN 总线` 改为 `RS485 / MODBUS RTU`，除非硬件另有 CAN 连接证据。
2. 在 TCB-NE 模块旁增加 `CTL: Ready / Alarm / TEC Enable`。
3. 在通道模板中增加 `NTC/PT100/PT1000 温度反馈`。
4. 在 24V 配电后增加 `每通道分路保护/电流检测`。
5. 在 JTT1031V3 旁增加 `DUT供电、I2C读写、pin-in/pin-power、电压电流、PRBS/BERT`。

优先级中：

1. 增加 `MODBUS地址 128-247，默认128，9600bps，轮询间隔>5ms` 注记。
2. 增加 `W-G短接: 纯MODBUS模式` 注记。
3. 增加 `RS485终端电阻 JP0/120Ω` 注记。
4. 明确两路 `LRS-600-24` 的实际供电分工。

